# 测试清单：`国内更新源及双源自动降级`

> 对应更改：新增国内更新源（腾讯云 COS+CDN），新增更新源切换 GUI，实现检查更新与下载的双源自动降级机制

## 测试环境

| 项目 | 信息 |
|------|------|
| AutoHotkey 版本 | `v2.0.26` |
| Windows 版本 | `Windows 11 25H2` |
| 测试日期 | `2026-07-13` |

---

## 单元测试

### 功能点：更新源下拉框

- [x] **操作**：打开 AFA 设置窗口 → "其他设置" → "更新"分类
- [x] **预期**："更新渠道"下方出现"更新源"下拉框，包含"GitHub"和"国内源"两个选项

- [x] **操作**：查看下拉框默认选中值
- [x] **预期**：默认选中"国内源"（索引 1）

### 功能点：Token 行联动灰化

- [x] **操作**：更新源下拉框切换为"国内源"
- [x] **预期**："使用 GitHub Token"复选框变为灰色不可用、Token 输入框变为灰色不可用、提示文字变为灰色

- [x] **操作**：更新源下拉框切回"GitHub"
- [x] **预期**：Token 复选框、输入框、提示文字恢复为可用状态

- [x] **操作**：更新源设为"国内源"，切换到其他分类（如"启动与退出"），再切回"更新"分类
- [x] **预期**：Token 行仍保持灰色不可用状态

### 功能点：配置持久化

- [x] **操作**：更新源设为"国内源"，点击"保存"，关闭设置窗口，重新打开设置窗口
- [x] **预期**："更新源"仍显示"国内源"

### 功能点：脏值检测

- [x] **操作**：切换更新源从"国内源"到"GitHub"
- [x] **预期**：底部出现"修改尚未保存或应用！"提示，"保存"和"应用"按钮变为可用

- [x] **操作**：切回"国内源"
- [x] **预期**：提示消失，"保存"和"应用"按钮恢复灰色

---

## 集成测试

### 流程：国内源检查更新（正常路径）

> 涉及模块：`config`、`version_checker`、`updater_manager`、`gui`

- [x] **前置**：CDN 上 `stable/version.json` 可正常访问，本地 AFA 版本为 v1.5.4
- [x] **操作**：更新源选"国内源"，更新渠道选"正式版"，点击"手动检查更新"
- [x] **预期**：提示"发现新版本 v1.5.5"，显示更新日志

### 流程：国内源检查——已是最新

> 涉及模块：`config`、`version_checker`、`updater_manager`

- [x] **前置**：CDN 上 `stable/version.json` 中 version 字段与本地版本号相同
- [x] **操作**：更新源选"国内源"，点击"手动检查更新"
- [x] **预期**：提示"已是最新版本"

### 流程：更新源切换后检查

> 涉及模块：`config`、`version_checker`、`gui`

- [x] **操作**：更新源先选"国内源"→ 手动检查更新，然后切换为"GitHub"→ 手动检查更新
- [x] **预期**：两次检查均正常返回结果，互不影响

### 流程：降级——国内源不可用时自动切 GitHub

> 涉及模块：`version_checker`、`updater_manager`

- [x] **前置**：修改 host 文件将 `release.arknightsframeassistant.com` 指向 `127.0.0.1` 模拟不可用
- [x] **操作**：更新源选"国内源"，点击"手动检查更新"
- [x] **预期**：国内源连接拒绝后自动降级到 GitHub 源，检查成功（降级日志已确认）

##### 异常路径

- [x] **异常**：断网使双源均不可达 → **预期**：提示"两个更新源均不可用，请检查网络连接"
- [x] **异常**：version.json 格式错误（缺少 version 字段）→ **预期**：国内源检查失败，自动降级到 GitHub

### 流程：下载降级——国内源下载失败切 GitHub

> 涉及模块：`updater_manager`、`downloader`

- [ ] **跳过**：需要发布新版本才能触发下载流程，下次实际更新时可验证

##### 异常路径

- [ ] **跳过**：双源下载均不可达（同上）

---

## 回归测试

### 功能：GitHub 源检查更新（未改动但相关）

- [x] **操作**：更新源选"GitHub"，更新渠道选"正式版"，点击"手动检查更新"
- [x] **预期**：正常检查更新（与旧版本行为一致）

### 功能：更新渠道切换（未改动）

- [x] **操作**：更新源选"GitHub"，更新渠道切换为"测试版"，点击"手动检查更新"
- [x] **预期**：从 GitHub pre-release 检查更新，行为正常

### 功能：GitHub Token 功能（未改动但相关）

- [x] **操作**：更新源选"GitHub"，勾选"使用 GitHub Token"，输入有效 Token，保存
- [x] **预期**：Token 正常保存和验证，下次检查更新时使用 Token

### 功能：自动检查更新（未改动）

- [x] **操作**：开启"自动检查更新"，重启 AFA
- [x] **预期**：启动后自动检查更新，行为正常

---

## 追加测试

> 对应更改：token_invalid/rate_limited 降级处理、下载取消处理、回归项补充

### 集成测试补充

#### 流程：token_invalid 手动检查 — 显示错误

> 涉及模块：`updater_manager`、`version_checker`、`updater_ui`

- [x] **前置**：更新源选"GitHub"，配置一个无效的 Token（如 `invalid123`），勾选"使用 GitHub Token"并保存
- [x] **操作**：手动点击"检查更新"
- [x] **预期**：显示 Token 无效的错误提示对话框

#### 流程：token_invalid 自动检查 — 静默降级

> 涉及模块：`updater_manager`、`version_checker`

- [x] **前置**：更新源选"GitHub"，已配置无效 Token，开启"自动检查更新"
- [x] **操作**：重启 AFA，等待自动更新检查完成
- [x] **预期**：无弹窗（静默降级到国内源）

#### 流程：rate_limited 手动检查 — 显示提示

> 涉及模块：`updater_manager`、`version_checker`、`updater_ui`

- [ ] **跳过**：需要短时间内频繁调用 API 触发配额限制，难以手动复现

#### 流程：下载取消后不触发降级

> 涉及模块：`updater_manager`、`downloader`、`updater_ui`

- [ ] **跳过**：需要触发实际下载流程，下次发布时验证

### 回归测试补充

#### 功能：已忽略版本功能

- [x] **验证**：检测到新版本时点击"忽略此版本"，重启 AFA 自动检查不再弹出同版本更新提示

---

## 测试结果

- [x] 全部通过（4 项跳过：下载降级×2、rate_limited、下载取消，均需实际发布场景验证）

## 问题反馈

无。
