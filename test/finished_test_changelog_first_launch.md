# 测试清单：`changelog_first_launch`

> 对应更改：修复从旧版本（无 changelog.json）升级到 v1.5.0-beta.1 时，首次启动不弹出更新公告的问题。当 changelog.json 不存在时，自动触发一次版本检查以生成缓存，然后弹出更新公告。

## 测试环境

| 项目 | 信息 |
|------|------|
| AutoHotkey 版本 | `2.0.26` |
| Windows 版本 | `Windows 11 25H2` |
| 测试日期 | `2026-05-30` |

---

## 单元测试

本次更改不涉及可独立测试的单一功能。新增的 `VersionChecker.FetchChangelogCache()` 方法和 `ChangelogChecker.CheckAndShow()` 的修改均在启动流程中自动触发，无法通过 GUI 控件单独验证。

---

## 集成测试

### 流程：从旧版本升级后首次启动（changelog.json 不存在）

> 涉及模块：`changelog_checker`、`version_checker`、`changelog_ui`

- [x] **前置**：关闭 AFA；删除 `%AppData%\ArknightsFrameAssistant\PC\changelog.json`（模拟旧版本升级场景）；确保网络可用
- [x] **操作**：启动 AFA
- [x] **预期**：启动后弹出"更新公告"窗口，窗口内显示版本发布内容（Markdown 格式），下方有"直到下次更新前不再弹出"复选框和"确定"按钮
- [x] **操作**：点击"确定"关闭公告窗口
- [x] **预期**：窗口正常关闭，AFA 托盘图标显示正常

##### 异常路径

- [x] **异常**：关闭 AFA；删除 `changelog.json`；**断开网络**；启动 AFA → **预期**：不弹出更新公告窗口，AFA 正常启动（静默跳过，不报错）
- [x] **异常**：关闭 AFA；删除 `changelog.json`；恢复网络；启动 AFA，勾选"直到下次更新前不再弹出"并点确定 → **预期**：重启 AFA 后不再弹出公告窗口

### 流程：正常启动（changelog.json 已存在）

> 涉及模块：`changelog_checker`、`changelog_ui`

- [x] **前置**：`changelog.json` 已存在（上面流程成功执行后即满足）；当前版本的公告未被关闭
- [x] **操作**：启动 AFA
- [x] **预期**：正常弹出更新公告窗口（与修改前行为一致）
- [x] **前置**：在上次启动时勾选了"直到下次更新前不再弹出"
- [x] **操作**：重启 AFA
- [x] **预期**：不弹出更新公告窗口（与修改前行为一致）

---

## 回归测试

### 功能：自动更新检查（AppStarted）

- [x] **前置**：确认设置中"自动检查更新"已开启
- [x] **操作**：启动 AFA，观察是否在约 100ms 后自动执行版本检查
- [x] **预期**：自动更新检查正常触发，行为与修改前一致

### 功能：手动更新检查

- [x] **操作**：在 AFA 设置窗口中点击"检查更新"
- [x] **预期**：手动检查更新功能正常，行为与修改前一致

### 功能：`changelog.json` 已存在时公告内容正确

- [x] **前置**：`changelog.json` 已存在且包含多个版本
- [x] **操作**：启动 AFA，观察公告窗口显示的内容
- [x] **预期**：公告窗口按版本降序显示所有版本的发布内容，版本间用 `---` 分隔，标题格式为 `## vX.Y.Z (YYYY-MM-DD)`

---

## 测试结果

- [x] 全部通过
- [ ] 存在问题（详见下方问题反馈）

## 问题反馈

### 问题1：[问题描述]
- [ ] 已解决
